# Copy to .env and fill in
# (or set these in Streamlit Cloud Secrets when deploying)

# The Odds API — get a free key at https://the-odds-api.com/
# Required ONLY for the live betting page; the rest of the dashboard works without it.
ODDS_API_KEY=
